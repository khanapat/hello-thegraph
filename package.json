{
  "name": "trust",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ trust",
    "create-local": "graph create --node http://localhost:8020/ trust",
    "remove-local": "graph remove --node http://localhost:8020/ trust",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 trust"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.0",
    "@graphprotocol/graph-ts": "0.22.0"
  }
}
